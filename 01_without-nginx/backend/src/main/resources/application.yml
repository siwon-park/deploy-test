server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:mysql://[도메인주소]:3306/testdb?serverTimezone=Asia/Seoul
    username: root
    password: 5041
    driver-class-name: com.mysql.cj.jdbc.Driver